spring:
  cloud:
    config:
      server:
        bootstrap: true
      uri: http://localhost:8888
      name: config-service
#      name: ecommerce
      #server:
        #native:
          #search-locations: file://${user.home}/...
#  profiles:
#    active: dev
    #active: native
