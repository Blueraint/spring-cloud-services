spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: ecommerce
      #server:
        #native:
          #search-locations: file://${user.home}/...
  profiles:
    active: dev
    #active: native
